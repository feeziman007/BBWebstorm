module.exports = function (grunt) {
    grunt.initConfig({

        jshint: {
            files: ['js/*.js']
        },

        ugligy: {

        }

    })

    grunt.loadNpmTasks()
    grunt.registerTask()
}
